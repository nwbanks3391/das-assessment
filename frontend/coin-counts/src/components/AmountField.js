import React from 'react';

const AmountField = (props) => {
    return (
        <div>
            Input placeholder
            Button placeholder
        </div>
    );
};

export default AmountField;